# Audit: Code Health Check

You are running cub's code health audit. This checks for dead code, documentation gaps, and coverage issues.

## Arguments

$ARGUMENTS

## Instructions

Run `cub audit` via Bash and present the results clearly.

If the user specified a focus area (e.g., "dead code", "docs", "coverage"), pass it as an argument: `cub audit <focus>`.
